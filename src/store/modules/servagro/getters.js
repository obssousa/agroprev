const getters = {
    getDailyModel(state) {
        return state.dailyModel
    },
    getInterpolation(state) {
        return state.interpolation
    },
    getADD(state) {
        return state.add
    }
}

export default getters